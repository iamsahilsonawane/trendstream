name: latest_movies
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cached_network_image: ^3.2.1
  hooks_riverpod: ^1.0.4
  flutter_hooks: ^0.18.4
  firebase_core: ^1.17.1
  firebase_auth: ^3.3.19
  shared_preferences: ^2.0.15
  flutter_vlc_player: ^7.1.5
  focus_notifier:
    git:
      url: https://github.com/iamsahilsonawane/focus_notifier.git
      ref: main
  firebase_crashlytics: ^2.8.2
  audio_video_progress_bar: ^0.10.0
  dio: ^4.0.6
  json_annotation: ^4.6.0
  equatable: ^2.0.4
  flutter_staggered_grid_view: ^0.6.2
  intl: ^0.17.0
  url_launcher: ^6.1.5
  # chewie: ^1.3.3
  # video_player: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.0
  json_serializable: ^6.3.1

flutter:
  uses-material-design: true
